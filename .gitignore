/target
.vai/
.cargo/
cargo-home/
build.sh
cargo-build.sh
*.swp
*.swo
.DS_Store

# Docker Compose local config (contains secrets)
infra/configs/server.toml

# Env files (any path, any suffix — e.g. .env, .env.local, .env.production).
# Explicitly keep .env.example / .env.sample trackable as templates.
.env
.env.*
!.env.example
!.env.sample

# Terraform state and secrets
infra/terraform/.terraform/
infra/terraform/.terraform.lock.hcl
infra/terraform/terraform.tfstate
infra/terraform/terraform.tfstate.backup
infra/terraform/terraform.tfvars

# Claude Code user-local state (settings, scheduled tasks, worktrees)
.claude/

# Playwright MCP test artifacts
.playwright-mcp/
